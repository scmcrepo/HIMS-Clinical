# HANDOFF — HMS Agentic Delivery

_Written 2026-08-31T13:56:01+00:00_

Read this after `ledger.py status`. Then read
`references/codebase-map.md` before touching code.

## What happened last session

WO-032: remediated F1-F4 from the independent DPDP review. V215 adds branch_id to erasure_requests, incident_affected_principals, grievance_events and the three V213 retention tables, and their entities are now declared tenant-wide - this is what made the s.8(7) retention engine, s.12 erasure, the s.8(9) grievance audit trail and the Rule 7 affected-principals list executable for the first time. Added AuditableEntitySchemaTest + check_entity_schema.py so the bug class cannot recur. Tenant onboarding now refuses without a publishable grievance contact. Consent derives minority from date of birth and refuses a contradiction. Base logging defaults are safe for a profile-less boot; hibernate jdbc.bind TRACE commented out in dev because it logged the plaintext going into the encrypted columns. All 203 migrations replay clean; nothing compiled.

## What to do next

1) Compile and boot: the acceptance test for F1 is that event=retention.startup.validation_failed stops appearing in application-error.log. 2) Run the new tests - AuditableEntitySchemaTest needs Docker. 3) Gate 1 on card X-006 (F5): decide whether the Areas feature is restored or deleted, and whether to add the three missing columns to customers. 4) Collect grievance contacts from the four existing tenants - the code change only stops the FIFTH tenant having the gap. 5) Still untouched and still open: MFA (U-002), key rotation (U-003), notice approval and Tamil translation, retention periods and arming (L-005), the fabricated-consent disposition.

## State at handoff

### WO-001 — Agent Gateway: auth, tool surface, audit  (IN_PROGRESS, 1/10 tasks)
- [>] T-001 Remove HmsPermissionEvaluator debug-file write
      last note: IMPLEMENTED, NOT VERIFIED. Removed Files.writeString block from HmsPermissionEvaluator.hasPermission; replaced with guarded log.debug. Added HmsPermissionEvaluatorTest (8 cases) incl. no-file-IO assertion and a case pinning the getAuthorities() fallback that T-005 agent scopes depend on. CORRECTION to card: evaluator_debug.log is NOT git-tracked (.gitignore already covers *.log); no git rm needed, left the local artifact alone. NEEDS: ./gradlew test.
- [>] T-002 Observability foundation: Micrometer, Prometheus, correlation-id MDC
      last note: CONFIRMED by direct run this session: the pre-existing frontend red is real and is exactly 2 tests in src/features/auth/pages/LoginPage.test.tsx. Full suite 258 passed / 2 failed / 13 files. Cause identified: the component sends forceLogout:false and toHaveBeenLastCalledWith does not expect it. git log dates the file to 2026-07-15, six weeks before the DPDP campaign. Worth fixing so it stops masking real regressions, but it is not campaign-caused.
- [>] T-003 V176 migration: agent tables, feature keys, AGENT role, tenant provisioning
      last note: IMPLEMENTED, NOT VERIFIED. V176__agent_gateway.sql: 3 tables (agent_api_tokens, agent_tool_invocations append-only, agent_idempotency_keys), 6 feature keys x all tenants, AGENT role x all tenants, grants. TenantService wired for future tenants. BUG FOUND AND FIXED DURING IMPLEMENTATION: V176 created AGENT with branch_id NULL but seedRbac would have created it branch-scoped, giving existing vs future tenants different role shapes; added AGENT to tenantWideRoles. Verified ROLE_GRANTS is 9 pairs (Map.of caps at 10) and added a test asserting it. NEEDS: Testcontainers run of the migration against a DB at V175 and a full replay from V001 -- no Postgres in this sandbox.
- [>] T-004 Agent token issuance, hashing, listing and revocation
      last note: STATIC VERIFICATION ADDED (still not compiled). tree-sitter-java parse of all 735 repo files: zero syntax errors, including every new agent file. All com.hms imports in the 34 target files resolve against the repo symbol table. Constructor arities cross-checked against real declarations: BookAppointmentRequest(10) and HmsUserDetails(11) both match. No invented record accessors. REMAINING RISK: external library imports (Spring/Jakarta/Micrometer/springdoc) and type correctness are unchecked - only javac can do that.
- [>] T-005 AgentTokenAuthenticationFilter and the stateless /agent filter chain
      last note: JAVA TESTS NOW WRITTEN (still not compiled). Added AgentGatewayIsolationIntegrationTest (14 cases, Testcontainers): auth accept/reject/revoke, NO Set-Cookie assertion for statelessness, scope 403 on write tool, cross-tenant ABSENCE of a token, per-tenant feature seeding, AGENT role tenant-wide, idempotency index per-tenant. Plus AgentTokenServiceTest (14) and AgentPrincipalFactoryTest (6).
- [>] T-006 Tool surface skeleton, error envelope, audit, metrics, and check_bed_occupancy
      last note: STATIC VERIFICATION ADDED (still not compiled). tree-sitter-java parse of all 735 repo files: zero syntax errors, including every new agent file. All com.hms imports in the 34 target files resolve against the repo symbol table. Constructor arities cross-checked against real declarations: BookAppointmentRequest(10) and HmsUserDetails(11) both match. No invented record accessors. REMAINING RISK: external library imports (Spring/Jakarta/Micrometer/springdoc) and type correctness are unchecked - only javac can do that.
- [>] T-007 Remaining tools: check_slot_availability, book_slot, fetch_billing_ledger
      last note: STATIC VERIFICATION ADDED (still not compiled). tree-sitter-java parse of all 735 repo files: zero syntax errors, including every new agent file. All com.hms imports in the 34 target files resolve against the repo symbol table. Constructor arities cross-checked against real declarations: BookAppointmentRequest(10) and HmsUserDetails(11) both match. No invented record accessors. REMAINING RISK: external library imports (Spring/Jakarta/Micrometer/springdoc) and type correctness are unchecked - only javac can do that.
- [>] T-008 Idempotency for book_slot
      last note: STATIC VERIFICATION ADDED (still not compiled). tree-sitter-java parse of all 735 repo files: zero syntax errors, including every new agent file. All com.hms imports in the 34 target files resolve against the repo symbol table. Constructor arities cross-checked against real declarations: BookAppointmentRequest(10) and HmsUserDetails(11) both match. No invented record accessors. REMAINING RISK: external library imports (Spring/Jakarta/Micrometer/springdoc) and type correctness are unchecked - only javac can do that.
- [>] T-009 Generated tool schema endpoint
      last note: STATIC VERIFICATION ADDED (still not compiled). tree-sitter-java parse of all 735 repo files: zero syntax errors, including every new agent file. All com.hms imports in the 34 target files resolve against the repo symbol table. Constructor arities cross-checked against real declarations: BookAppointmentRequest(10) and HmsUserDetails(11) both match. No invented record accessors. REMAINING RISK: external library imports (Spring/Jakarta/Micrometer/springdoc) and type correctness are unchecked - only javac can do that.
- [x] T-010 Agent token management UI

### WO-002 — FHIR R4 serialization layer  (IN_PROGRESS, 0/2 tasks)
- [>] F-001 FHIR R4 resource builders (Patient, Coverage, Encounter, Organization, Practitioner)
      last note: IMPLEMENTED, NOT COMPILED. password_reset_otp.email now encrypted, with email_token holding a deterministic HMAC for lookup - the patients.contact_number_token pattern. Repository REWRITTEN: findFirstByEmailAndOtp removed rather than deprecated, because leaving it would leave a working-looking method that always returns empty against a non-deterministic column. V212 widens email to TEXT, adds the token column, and DELETES all rows: they have a 5-minute TTL, and migrating them risks a half-encrypted auth table that locks users out. Cost is that anyone mid-reset presses resend. ALSO FIXED while here: the reset flow called otpRepo.findAll().stream().filter() to clear old OTPs - loading every row in the table into memory, which once the column was encrypted would have decrypted every row on every reset request. Now a single deleteByEmailToken. STILL OPEN: the tenant-scoping question for this table (two tenants with a user at the same address) is unreasoned and recorded in the entity comment, not resolved.
- [>] F-002 Bundle assembly + validation for ABDM and NHCX profiles
      last note: IMPLEMENTED, NOT COMPILED. migrateVisits, migrateNhcxTransactions, migratePharmacySales added to PiiMigrationRunner and WIRED INTO migratePii() - a test asserts the wiring, because an orphaned migration method is exactly as useless as no method and that is the ErasureService.sweep() failure repeating. V212 adds the pii_encrypted progress flags the batch SELECTs depend on; without them the migration aborts at startup on a missing column. All three use encryptIfPlaintext (checks looksEncrypted first) not enc.encrypt, so a re-run is a no-op rather than double encryption - which would be unrecoverable. Partial indexes added so the batch SELECT does not scan the whole table once most rows are done. NEEDS: rehearsal on a restored copy plus a read-back of a historical row in each table.

### WO-003 — ABDM/ABHA integration + central government token auth  (IN_PROGRESS, 0/3 tasks)
- [>] A-001 Government API credential config + central bearer token manager
      last note: IMPLEMENTED, NOT COMPILED. GovApiProperties (all credentials externalised, no working defaults - a default pointing at a sandbox is how test creds reach prod) + GovTokenManager (per-provider caching, refresh skew, double-checked locking, invalidate-on-401). Token refresh failures log only the provider and exception type: the message can contain the client secret.
- [>] A-002 ABDM client: ABHA creation, OTP, existence check, profile
      last note: IMPLEMENTED, NOT COMPILED. AbdmClient: requestAadhaarOtp, requestMobileOtp, verifyOtpAndEnrol, abhaAddressExists. Aadhaar is passed through and never stored, never logged, never in an exception message. Error paths log status code only because ABDM response bodies echo the submitted Aadhaar. CAVEAT: ABDM has revised its API across v1/v2/v3; paths reflect the v3 sandbox shape and are configurable. CLIENT MUST SUPPLY: sandbox credentials + facility id + certification.
- [>] A-003 ABHA linkage persistence + service + agent tool
      last note: IMPLEMENTED, NOT COMPILED. AbhaLinkageEntity + V178. ABHA number/address encrypted with blind-index token columns (encrypted columns are not searchable). No Aadhaar column at all. DPDP consent recorded separately from ABDM's own consent artefact.

### WO-004 — Agent service foundation: LangGraph supervisor, state, HMS client  (COMPLETE, 5/5 tasks)
- [x] G-001 Graph state, supervisor routing, confidence thresholds
- [x] G-002 HMS gateway client: retries, idempotency, correlation
- [x] G-003 PII redaction + structured observability
- [x] G-004 Data-residency guard + settings
- [x] G-005 LLM classifier with rule fallback

### WO-005 — Sub-agents: ABHA reception, scheduling/triage, TPA claims  (COMPLETE, 4/4 tasks)
- [x] S-001 Scheduling/triage sub-agent
- [x] S-002 Billing sub-agent
- [x] S-003 ABHA reception sub-agent (full flow against AbdmClient)
- [x] S-004 TPA claims sub-agent (full flow against NhcxClient)

### WO-006 — WhatsApp channel  (COMPLETE, 2/2 tasks)
- [x] W-001 Webhook signature verification, dedupe, 24h window rules
- [x] W-002 BSP transport + FastAPI webhook endpoint + template registry

### WO-007 — Voice pipeline: streaming STT/TTS, barge-in, latency budget  (COMPLETE, 2/2 tasks)
- [x] V-001 Latency budget accounting + barge-in state machine
- [x] V-002 Telephony ingress + streaming STT/TTS providers

### WO-008 — NHCX native claims track  (IN_PROGRESS, 0/3 tasks)
- [>] N-001 NHCX client: signed/encrypted transport + correlation persistence
      last note: CODEC NOW IMPLEMENTED (not compiled). NhcxPayloadCodec no longer throws: real JWS(RS256)-then-JWE(RSA-OAEP-256/A256GCM) via nimbus-jose-jwt, per-payer public keys resolved by participant code, key caching. decryptAndVerify THROWS on signature failure - a payload that decrypts but does not verify is an unauthenticated claim response.
- [>] N-002 Eligibility + preauth + claim bundle builders
      last note: IMPLEMENTED via ClaimBundleBuilder (see F-002).
- [>] N-003 Async callback endpoint + idempotent status handling
      last note: CALLBACK NOW IMPLEMENTED (not compiled). NhcxCallbackController (permitAll - NHCX has no session with us; auth is the JWS signature) + NhcxCallbackService + repository. Duplicate callbacks are no-ops via isAwaitingCallback. Tenant resolved from the correlation id AFTER verification, then set explicitly and cleared in finally. expireStalled escalates submissions the payer never answered. Always 202 on well-formed requests so a downstream error cannot cause an NHCX retry storm.

### WO-009 — Legacy TPA RPA track (Playwright worker pool)  (COMPLETE, 2/2 tasks)
- [x] R-001 RPA worker: Playwright driver abstraction + credential vault interface
- [x] R-002 TPA portal flow runner with session recording + escalation

### WO-010 — HITL interrupts + Administrative Copilot dashboard  (IN_PROGRESS, 2/5 tasks)
- [x] H-001 V177: hitl_escalations table + HITL_MANAGE feature + provisioning
- [>] H-002 Escalation entity, repository, service (raise/list/resolve)
      last note: LEDGER REPAIR NOTE: this card was originally created as G-002, colliding with WO-004's existing G-002. That collision regressed WO-004 from COMPLETE to IN_PROGRESS and attached WO-028 evidence to its cards. Repaired: WO-004 restored to COMPLETE with all 5 cards DONE, WO-028 cards renamed G-* to H-*, notes reattached here.
- [>] H-003 /api/hitl controller: queue, detail, decision
      last note: IMPLEMENTED, NOT COMPILED. HitlController (/hitl, HITL_MANAGE) queue/detail/decision/count - queue returns summaries WITHOUT transcripts to limit PHI exposure. AgentHitlController (/agent/v1/hitl, AGENT_HITL_RAISE) for filing. Deliberate scope split: an agent can ask for help, never resolve its own request for help. Tenant comes from the token, never the body.
- [x] H-004 Agent-side HITL client + graph wiring
- [>] H-005 Escalation timeout job + stale alerting
      last note: IMPLEMENTED, NOT COMPILED. 13 historical migrations V012-V195 seeded features without iterating tenants, so a tenant created before a feature existed silently has no row and every hasPermission returns false - invisible feature, no error. Migrations are immutable so V208 backfills forward, taking the union of all keys. Role GRANTS deliberately not backfilled: which roles hold which feature is a per-hospital decision and guessing would hand out permissions nobody chose.

### WO-011 — Shadow mode, compliance hardening, phased rollout  (IN_PROGRESS, 3/4 tasks)
- [x] P-001 Shadow mode: proposal capture + store
- [x] P-002 Agreement scoring job + go-live dashboard
- [>] P-003 DPDP consent model + erasure/retention jobs
      last note: JAVA TESTS NOW WRITTEN. ErasureServiceTest asserts every patient-data store is in the erasure registry and that consent_records is swept last so a partial failure cannot destroy the audit trail. ConsentPurposeTest pins that only TREATMENT is requiredForCare.
- [x] P-004 Phased rollout switches + runbook

### WO-012 — Module 1 — ABHA verification/creation REST surface + patient badge + card print  (IN_PROGRESS, 2/6 tasks)
- [>] AB-001 ABHA application service + repository + REST controller + DTOs (Screen 1.1 backend)
      last note: IMPLEMENTED, NOT COMPILED. Created AbhaLinkageJpaRepository (blind-index lookups only; deliberately NO findByAbhaNumber since the column is non-deterministically encrypted and equality would silently return nothing), AbhaService (DPDP ABHA_LINKAGE consent gate fires BEFORE any gateway call; duplicate-linkage guard; failure recorded as exception TYPE NAME not message because ABDM error bodies echo Aadhaar), AbhaController @PreAuthorize hasPermission('ABHA_MANAGE','') - feature key ALREADY seeded by V178 so NO new migration needed, AbhaLinkageResponse masks to XX-XXXX-XXXX-nnnn, 2 request DTOs, and AbhaServiceTest (14 cases incl. Aadhaar-never-persisted, consent-before-gateway, mask correctness). STATIC VERIFICATION: tree-sitter parsed 778 repo java files, 0 syntax errors; all com.hms imports in the 7 new files resolve against the repo symbol table; AuditableEntity.setId/PiiSearchTokenService.token/ConsentRequiredException(purpose) signatures cross-checked. NEEDS ./gradlew test - no javac and Maven Central 403 in sandbox.
- [x] AB-002 ABHA frontend domain types, validation, badge/duplicate guards + API client
- [x] AB-003 ABHA verification modal component + patient-master badge wiring
- [ ] AB-004 ABHA card download/print endpoint + template (separately permissioned, audited)
- [ ] AB-005 Aadhaar demo-auth fallback path in AbdmClient
- [>] AB-006 Screen 1.3 manual policy: memberId/tpaName/policyType wired through entity, DTO, service, frontend cmd
      last note: IMPLEMENTED, NOT COMPILED (backend half). Added memberId (encrypted) + memberIdToken (blind index) + tpaName + policyType to the Insurance entity, CreateInsuranceRequest and InsuranceService; extended CreateInsuranceCmd on the frontend (tsc clean). AVOIDED A REGRESSION: I first added a server-side rule requiring either a policy number or a member id. Checked the callers and found InsurancePage.tsx enables Create Record on insurerName alone - the existing screen legitimately creates a record while paperwork is still being chased, so the new rule would have broken a screen in current use as a side effect of adding a new one. Reverted it; the either-identifier rule lives in validateManualPolicy (Screen 1.3 form) where the requirement actually applies. No Java caller constructs CreateInsuranceRequest directly so the record arity change is safe.

### WO-013 — Module 1/2 — NHCX policy discovery, OTP authorisation, coverage & benefit storage  (IN_PROGRESS, 4/6 tasks)
- [>] PD-001 V191 migration: discovered_policies, patient_policy_coverages, exclusions, manual-policy columns, POLICY_DISCOVERY feature
      last note: IMPLEMENTED, NOT MIGRATED. V191__policy_discovery_and_coverage.sql. NEXT FREE VERSION IS V191 (repo shipped to V190 since July; V180 would have collided). Adds discovered_policies (payer assertion, kept separate from insurances until a human links it), patient_policy_coverages (13 benefit fields, append-only snapshots so the admission-time answer survives a later dispute), policy_benefit_exclusions, plus member_id/member_id_token/tpa_name/policy_type on insurances for Screen 1.3. MONEY IN PAISE BIGINT; co-pay in BASIS POINTS not percent because retail policies carry 7.5%. CHECK constraints reject negative balances and co-pay outside 0-10000bp. POLICY_DISCOVERY feature seeded per tenant and granted to whichever roles already hold NHCX_CLAIMS. Verified role_features has pk (role_id,feature_id) so ON CONFLICT DO NOTHING is valid, and features has uq_features_tenant_key. NEEDS Testcontainers replay.
- [x] PD-002 CoverageResponseParser: FHIR benefits to paise/basis-points, missing-vs-zero
- [>] PD-003 PolicyDiscoveryService + NhcxClient discovery/OTP + REST controller + DTOs
      last note: IMPLEMENTED, NOT COMPILED. PolicyDiscoveryService (OTP required before registry lookup - querying a person's insurance holdings without authorisation is a DPDP problem regardless of what the gateway allows; INSURANCE_CLAIM consent gate; discovery idempotent on correlationId so a gateway retry does not double the desk's list), NhcxClient +discoverPolicies/requestDiscoveryOtp/confirmDiscoveryOtp, PolicyDiscoveryController @PreAuthorize POLICY_DISCOVERY, 2 request + 2 response DTOs. Responses mask policy number and member id to ****nnnn. Amounts stay in paise to the browser: one rounding point, not two. Endpoints return correlationId not answers - NHCX is async.
- [x] PD-004 Frontend policy types: benefit formatting, co-pay split, admission guards, manual form
- [x] PD-005 NHCX callback wiring for discovery + on_check into PolicyDiscoveryService
- [x] PD-006 Screen 1.2/2.1 React components + Screen 2.2 benefit print template

### WO-014 — Module 3 — ABDM Consent Manager (HIU): consent artifact, data streaming, external records viewer  (IN_PROGRESS, 4/6 tasks)
- [>] MC-001 V195 migration: abdm_consent_requests, abdm_consent_artifacts, external_health_records, 2 feature keys
      last note: IMPLEMENTED, NOT MIGRATED. V195. THREE tables kept distinct on purpose: abdm_consent_requests (what we asked), abdm_consent_artifacts (what the CM signed and granted, one per granting HIP), external_health_records (what came back, encrypted, carrying the artifact id that authorised it so a revoked consent traces to everything it let in). CRITICAL: these are NOT consent_records - that is the hospital's DPDP register of its own lawful basis. An ABDM artifact is CM-issued, CM-signed, scoped to hi_types + a date range, and EXPIRES. Conflating them means either treating DPDP consent as authority to pull a stranger's records, or letting an expired artifact look like standing permission. CHECK constraint rejects a reversed date range which would silently fetch nothing. Feature keys ABDM_CONSENT_REQUEST + ABDM_RECORDS_VIEW granted to clinicians only.
- [x] MC-002 ConsentArtifactRules: expiry/revocation/scope gating (dependency-free)
- [>] MC-003 AbdmConsentClient (HIU) + AbdmConsentService + controller + DTOs + 3 entities/repos
      last note: IMPLEMENTED, NOT COMPILED. AbdmConsentClient kept SEPARATE from AbdmClient (enrolment) - different gateway surface and lifecycle; merging them means an identity-creation change can break record retrieval. AbdmConsentService: validates locally BEFORE calling the CM because the CM forwards nonsense to the patient and the hospital gets one approval interaction per request; recordGrant idempotent on artifact id; recordRevocation logs how many records the artifact had already admitted since that is the question asked immediately after and cannot be reconstructed later; fetchRecords DROPS anything outside the consented hi_types or date range because a HIP that over-shares is not authority to keep what it sent, and counts the drops as a warning metric; recordsFor filters against LIVE artifact validity not stored state so a time-boxed grant actually stops showing records. Every read audited to pii_disclosure_audit (reusing V193 as planned) incl. DENIED outcomes. openRecord audited separately from the index - that is when PHI is actually read. ExternalRecordResponse OMITS the payload so a 30-record list does not ship 30 decrypted clinical bundles and bypass per-open auditing.
- [x] MC-004 Frontend abdm types: consent form validation, expiry derivation, record grouping
- [x] MC-005 ABDM callback routes for on-notify/on-fetch wired to AbdmConsentService
- [x] MC-006 Screen 3.1 consent modal + Screen 3.2 records viewer tab in case sheet

### WO-015 — Module 4 — Cashless pre-auth submission, query response, enhancement  (IN_PROGRESS, 4/6 tasks)
- [>] PA-001 V196 migration: estimate lines, query thread, enhancements, ICD-10 table, preauth columns
      last note: IMPLEMENTED, NOT MIGRATED. V196. Estimate stored as LINES not a total: an insurer approving 80k against a 100k estimate has disallowed something specific, and without lines the Screen 4.4 enhancement becomes 'send more money' rather than 'the implant was costed at 40k and you allowed 20k' - the lines ARE the argument. Queries are a THREAD (unique on txn+round) because insurers raise multiple rounds and one column would overwrite the first question with the second. CHECK constraint ck_query_response forces responded_at and response_text to be set together, otherwise nobody can tell whether the insurer is still waiting on us. ck_enh_increase rejects an enhancement below what is already approved - that is a data entry error and the correct action is a claim. icd10_codes is DELIBERATELY EMPTY: ICD-10 is WHO/MoHFW published and a hand-written partial list would look authoritative while silently missing the diagnosis a clinician needs; search degrades to 'no matches' until the official release is loaded (PA-005). ALSO added insurance_id to nhcx_transactions - it was missing, so a claim could be filed with no recorded link to the coverage it relied on.
- [x] PA-002 PreAuthEstimateCalculator: line extension, room shortfall, patient liability, enhancement delta
- [>] PA-003 PreAuthService + entities/repositories + controller + 6 DTOs
      last note: Callback dispatch now reaches PreAuthService.recordQuery and recordEnhancementOutcome (previously unreachable). A query outcome maps to recordQuery, NOT to a rejection: closing a pre-auth the insurer is still considering makes the hospital resubmit instead of answering, restarting the clock on an admitted patient. ALSO FIXED A CONVENTION/RISK DEFECT FROM THE PREVIOUS SESSION: I had written PreAuthJpaRepositories and AbdmConsentJpaRepositories as nested interfaces inside a final class. NO other repository in this codebase uses that shape - all 40+ are top-level - and Spring Data scanning of nested repository interfaces was an unnecessary risk on top of the convention break. Flattened into 6 top-level interfaces and updated all references. Verified no injection cycle: none of ClaimPaymentService, PreAuthService or PolicyDiscoveryService depends back on NhcxCallbackService.
- [x] PA-004 Frontend preauth types: estimate maths, form validation, query thread helpers
- [x] PA-005 ICD-10 dataset loader + search endpoint (table seeded from the official release)
- [x] PA-006 Screens 4.1-4.4 React: estimate builder, status tracker, query modal, enhancement form

### WO-016 — Module 5 — Final claim, PaymentNotice, UTR/TDS bank reconciliation, control tower  (IN_PROGRESS, 5/7 tasks)
- [>] CP-001 V192 migration: financial_state lifecycle, claim_payment_advices (UTR/TDS), claim_deduction_lines, CLAIM_PAYMENTS feature
      last note: IMPLEMENTED, NOT MIGRATED. V192__claim_payments_and_reconciliation.sql. Adds financial_state as a NEW column rather than widening state: state tracks the NHCX exchange, financial_state tracks whether the hospital has been paid, and a claim can be exchange-complete and financially unpaid for weeks. All 5 statuses from the flow doc now exist incl. the 3 that were missing. claim_payment_advices keeps the payer assertion (net_disbursed_amount) SEPARATE from the hospital confirmation (bank_credited_amount) because comparing them is the entire purpose of Screen 5.3. UNIQUE (tenant_id, utr_number) so a duplicate advice cannot credit the ledger twice. claim_deduction_lines itemised so billing can dispute a specific line. Backfills financial_state for existing CLAIM/PREAUTH rows so the control tower is not empty on day one. NEEDS Testcontainers replay.
- [x] CP-002 ClaimSettlementCalculator: disallowance, co-pay split, net payable, reconciliation gap, state machine
- [>] CP-003 ClaimPaymentService + entities/repositories + control tower REST controller + DTOs
      last note: IMPLEMENTED, NOT COMPILED. ClaimPaymentService (recordPaymentAdvice idempotent on UTR - gateways deliver at least once and a duplicate crediting the ledger twice overstates receipts by the payment value; rejects an advice with no UTR since it would be unmatchable to a bank line; reconcile() records a mismatch rather than refusing it, moving the claim to CLAIM_DISPUTED so someone chases it, because the money DID arrive just not the advised amount), 2 entities, 2 repositories, ClaimPaymentController guarded by CLAIM_PAYMENTS not NHCX_CLAIMS so the person filing claims is not also the person certifying payment, 3 DTOs. CAUGHT AND FIXED A REAL DEFECT: I had invented com.hms.infrastructure.security.CurrentUser which does not exist; the import-resolution check caught it and it now uses SpringSecurityAuditorAware, the same rule that stamps created_by. NOTE: verifier reports one false-positive unresolved import (nested record ClaimSettlementCalculator.Split) - javac accepted that exact import in the harness, the checker just does not resolve nested types.
- [x] CP-004 Frontend control tower types: 5 metric cards, reconciliation, attention queue
- [x] CP-005 PaymentNotice callback route wired to ClaimPaymentService.recordPaymentAdvice
- [x] CP-006 Screen 5.1/5.2/5.3 React components + final claim submission UI
- [x] CB-001 NhcxCallbackRouter: dependency-free routing resolver for all NHCX callback payloads

### WO-017 — Phase 9 — Portal identity: OTP auth, cross-tenant lookup, portal principal  (IN_PROGRESS, 0/6 tasks)
- [>] PT-001 V197 migration: portal_otp_challenges, portal_sessions, patients.self_registered, PORTAL_* features + role + tenant provisioning
      last note: V197__patient_portal_identity.sql (portal_otp_challenges, portal_sessions, patients.self_registered, PORTAL_IDENTITY + PORTAL_PATIENT features, PORTAL_PATIENT role, grants) + Patient.selfRegistered field + TenantService wiring. BUG CAUGHT DURING WRITING: my lookup query used 'status = 0' citing EntityStatus.ACTIVE. The enum is INACTIVE(0), ACTIVE(1), DELETED(2) — 0 is INACTIVE, so the cross-tenant lookup would have matched only deactivated patients and every login would have reported 'no records found'. Fixed to status = 1 with a comment naming the trap. SECOND FINDING: ROLE_GRANTS in TenantService used Map.of(), which caps at 10 pairs and already held 9 with a comment predicting this exact moment; adding PORTAL_PATIENT would have hit the ceiling, so the map is now Map.ofEntries() and the stale warning comment is replaced. VERIFIED: tree-sitter parse of 868 files clean, all com.hms imports resolve. NOT VERIFIED: migration never replayed (no Postgres in sandbox); Flyway checksum, ON CONFLICT targets and the roles expression-index guard are unexecuted.
- [>] PT-002 OTP issue/verify service over NotificationPort, hashed codes, TTL, attempt cap, rate limits
      last note: PortalOtpService + PortalOtpChallengeEntity/Repository + PortalProperties + PortalOtpStartupCheck. BCrypt(10) for codes not SHA-256 (6 digits is ~20 bits; a fast hash over a dump is instant). Attempt counter incremented BEFORE comparison so aborting the connection still burns a guess. Challenge is bound to the number: verify() rejects a challengeId whose token does not match the submitted mobile, otherwise a caller could request a code to their own phone and present its id with someone else's number. generateCode uses per-digit sampling rather than nextInt(900000)+100000, which silently drops a tenth of the keyspace. Rate limits on both number and source. VERIFIED: syntax + imports. NOT VERIFIED: no compile, no runtime, no SMS send.
- [>] PT-003 Cross-tenant lookup: single filter-free ids-only query + per-tenant enrichment pass
      last note: PortalPatientLookupRepository (the single deliberate nativeQuery, ids-only projection) + PortalLookupService (two-pass: unfiltered id lookup, then per-tenant enrichment inside PortalTenantScope so decrypting reads stay tenant-scoped) + PortalTenantScope. check_conventions flags [tenant.native-query] on this file — that is the intended finding, not a defect: the file's javadoc, the ids-only projection and PortalSecurityInvariantsTest#crossTenantLookupReturnsIdsOnly are the controls. Also fixed branchRepo.findByTenantId -> findAllByTenantIdAndStatus (the former does not exist). Lookup timer tagged by found=true/false so the timing oracle is monitorable. VERIFIED: syntax + imports. NOT VERIFIED: the native SQL has never touched a database.
- [>] PT-004 Portal JWT, PortalPrincipalFactory, PortalTokenAuthenticationFilter, third SecurityFilterChain on /portal/**
      last note: PortalTokenService (nimbus-jose-jwt, already a dependency from N-001 — no new library), PortalPrincipalFactory, PortalTokenAuthenticationFilter, third SecurityFilterChain @Order(2) on /portal/** with the human chain moved to @Order(3). MAJOR DESIGN CORRECTION MID-IMPLEMENTATION: I first wrote the identity principal as an HmsUserDetails with a null tenant. Re-reading TenantResolutionFilter showed it responds to an authenticated non-superadmin with no tenant by sending 403 'No tenant assigned' and ending the request — every identity-scope endpoint would have failed. Replaced with a distinct PortalIdentityPrincipal record that the filter's instanceof check skips, and PortalTenantScope now establishes scope explicitly where a write needs it. Pinned by PortalSecurityInvariantsTest#identityPrincipalIsNotUserDetails. Patient principal always carries a branch, because HmsUserDetails.isHospitalAdmin() is true for tenant-without-branch — a missing branch would silently grant every branch in the hospital. VERIFIED: syntax + imports. NOT VERIFIED: no compile; Spring chain ordering and @PreAuthorize evaluation are unexercised.
- [>] PT-005 Session exchange (identity token -> patient token) + rotating refresh with reuse detection
      last note: PortalAuthService (establishSession re-derives the candidate set from the OTP-verified number rather than trusting the request; rotating refresh; reuse detection revokes the whole chain via parent_id; device cap revokes oldest chain rather than refusing the new login) + PortalAuthController + PortalExceptionHandler + PortalResponses/PortalRequests. Refresh tokens are opaque 256-bit random, not JWTs, because a self-contained token cannot be revoked and reuse detection needs a row. SHA-256 for refresh hashes (no brute-forceable structure, on the hot path) vs BCrypt for OTP codes. CORRECTION: first draft had @GetMapping('/../hospitals') and @PostMapping('/../patients/register') — invalid Spring mappings and dead code; extracted into PortalDirectoryController with correct base paths. VERIFIED: syntax + imports. NOT VERIFIED: no compile, no runtime.
- [>] PT-006 Self-registration endpoint, selfRegistered flag, per-number cap, DPDP consent capture
      last note: PortalRegistrationService + PortalDirectoryService + PortalDirectoryController. Delegates to PatientManagementService.registerPatient so portal registrations get the same number sequence, duplicate check and HMAC maintenance as the desk. Submitted mobile must hash to the OTP-verified token, so a caller cannot verify their own number and register against someone else's. Cap counted cross-tenant on the HMAC token (per-tenant would be defeated by registering once at each hospital). Registration runs inside PortalTenantScope because AuditableEntity @PrePersist stamps tenant_id from TenantContext and an identity-token caller has none — without it the new patient would be written with a null tenant and be invisible to every subsequent query. FINDING FED BACK TO THE APP: RegisterPatientRequest enforces ^[a-zA-Z\\s]+$ (letters and spaces only), but the PRD §4.2 specified ^[a-zA-Z][a-zA-Z.-\\s]*$ allowing dots and hyphens. mobile/src/core/validation.ts had implemented the PRD version, so 'Jean-Pierre' and 'St. John' would have passed on the phone and failed on submit. Client corrected to match the server; test rewritten to assert the real constraint. VERIFIED: syntax + imports; mobile suite 122/122 green after the change.

### WO-018 — Phase 9 — Portal data surface: consultants, slots, appointments, visit records  (CONFIRMED, 0/7 tasks)
- [ ] PS-001 PortalAccessGuard + GET /portal/me + portal_access_audit (V198)
- [ ] PS-002 Consultant listing + availability re-projection for the branch in the token
- [ ] PS-003 Booking with mandatory Idempotency-Key, 14-day window, slot capacity errors
- [ ] PS-004 Appointment list, reschedule, cancel with the 2h window and domain rules
- [ ] PS-005 Visit list + visit detail assembled inside the service transaction
- [ ] PS-006 Casesheet rendering data + approved-only lab and radiology reports
- [ ] PS-007 Attachments: metadata, 5-min signed URLs, download audit, staff self-registered badge

### WO-019 — Phase 9 — Patient mobile app (iOS + Android, Expo/React Native)  (IN_PROGRESS, 2/8 tasks)
- [x] M-001 Mobile workspace, tooling, design tokens, i18n scaffold, core API contracts
- [>] M-002 API client: transport, error envelope, correlation id, single-flight refresh, secure token store
      last note: PARTIALLY VERIFIED. core/http.ts, core/session.ts, core/api.ts, core/errors.ts, core/cachePolicy.ts are covered by http.test.ts (26) + session.test.ts (18) and typecheck clean — incl. single-flight refresh collapsing 5 concurrent callers to 1 network call (the case that would otherwise trip WO-017's refresh-reuse revocation against the patient's own device), transient-vs-rejected refresh distinction, and a PortalApi test asserting no read path sends a patientId query param. NOT VERIFIED: state/secureTokenStore.ts and state/container.ts import expo-secure-store / expo-constants / expo-crypto, which are not installed in this sandbox (the Expo SDK is a ~500MB install with native modules that cannot run headless here). They are written but have never been compiled. NEEDS: 'npm install && npx tsc --noEmit' on a dev machine with the Expo SDK present.
- [x] M-003 Resolution state machine + booking rules as pure tested logic
- [>] M-004 Auth screens: login, OTP, hospital, profile, branch selection
      last note: IMPLEMENTED, NOT COMPILED. app/_layout.tsx (container in useMemo so fast-refresh cannot spawn two SessionManagers racing to refresh the same token — that would look exactly like the reuse WO-017 treats as theft; queryClient.clear() on session loss so a shared handset does not show the next patient the previous one's cached clinical responses), (auth)/login|verify|hospital|profile|branch.tsx, (tabs)/_layout.tsx + index.tsx dashboard, ui/components.tsx, state/authStore.ts. CORRECTION MADE DURING IMPLEMENTATION: the tabs layout initially registered appointments/visits/settings Tabs.Screen entries whose route files do not exist yet — expo-router throws at startup on that, so the app would not have launched at all. Trimmed to index only, with a comment naming which card adds each one back. NEEDS: npm install with the Expo SDK present, then npx tsc --noEmit, then a simulator run.
- [>] M-005 Dashboard + appointments list + cancel/reschedule
      last note: PARTIAL: dashboard only (profile header, upcoming appointments via core/booking.upcomingAppointments, last 5 visits). The appointments tab with cancel/reschedule is NOT written. Note for whoever picks this up: canCancel/canReschedule in core/booking.ts already exist and are tested, so the screen is presentation over settled rules — do not re-derive the 2h window in the component.
- [ ] M-006 Booking flow: consultants, slot grid, confirmation, success
- [ ] M-007 Visit history, 4-tab visit detail, report viewer, FLAG_SECURE
- [ ] M-008 Self-registration, settings, consent withdrawal, local wipe, release checklist

### WO-020 — Phase 10 — Manual TPA insurance desk: 7-stage progressive workflow  (IN_PROGRESS, 2/7 tasks)
- [>] ID-001 V199 migration: 7-stage columns, cheque receipts table, REPORT_INSURANCE feature, tenant provisioning
      last note: IMPLEMENTED, UNVERIFIED. V199 written: 55 additive columns on insurances, insurance_cheque_receipts table, REPORT_INSURANCE feature seeded for existing tenants, TenantService.FEATURES + HOSPITAL_ADMIN grants wired for future tenants. Caught during implementation: the requirement doc said V197 (taken by patient portal; V198 reserved by WO-018) and used roles.role_key, which does not exist - V001 defines roles(id, name, ...). Switched to V192's UPPER(r.name) form. CANNOT VERIFY: no database in this environment and gradle cannot bootstrap (services.gradle.org blocked). check_conventions flags migration.destructive - false positive from the DROP COLUMN text in the documented rollback comment and the DROP CONSTRAINT IF EXISTS / ADD CONSTRAINT idempotency pattern copied from V192. The migration is purely additive.
- [>] ID-002 Domain: InsuranceWorkflowStage state machine + stage enums + Insurance entity extension
      last note: IMPLEMENTED, UNVERIFIED. InsuranceWorkflowStage (9 stages, monotonic advance, rank-based with tied ranks for approve/reject), ModeOfCommunication, ModeOfDispatch, CourierVendor, TpaDecision. Insurance entity extended with all 7 stages + effectiveApprovedLimit/isCardExpired/isBillLinked/advanceStage. InsuranceChequeReceipt entity + repo. 23-case InsuranceWorkflowStageTest written but NOT RUN - gradle unavailable. verify_java_static.py: 891 files parse, all com.hms imports resolve.
- [>] ID-003 InsuranceDeskService: 7 stage submissions, bill-link fix, date-range fix + controller + DTOs
      last note: IMPLEMENTED, UNVERIFIED. InsuranceDeskService with all 7 stage submissions, 7 request DTOs + 3 response DTOs, controller wiring. Both stubs fixed: updateBillId now persists (was parse-then-return-unchanged), getByDateRange now filters (was ignoring both params, returning getPending()). Corrected the work order: bill-not-linked returns 400 not 409, because GlobalExceptionHandler maps BusinessRuleViolationException to 400 and a 409 would need new shared exception plumbing. ~30-case InsuranceDeskServiceTest written but NOT RUN.
- [>] ID-004 Cheque receipts entity/repo + Stage 7 disallowance sync through BillingOperationsService
      last note: IMPLEMENTED, UNVERIFIED. Cheque reconcile-by-id (update/insert/delete-by-absence) in InsuranceDeskService.reconcileCheques. Stage 7 disallowances routed through the EXISTING BillingOperationsService.updateDisallowedAmounts rather than a second writer to charge_line_items.disallowed_amount (decision D-3). Tested via ArgumentCaptor in the unrun test class.
- [>] ID-005 V200 print templates: LETTER_ACCEPTANCE + ENHANCEMENT_REQUEST + PrintServiceImpl model builders
      last note: IMPLEMENTED, UNVERIFIED. V200 seeds LETTER_ACCEPTANCE and ENHANCEMENT_REQUEST print templates per tenant, following V194's exact INSERT..SELECT FROM tenants WHERE NOT EXISTS pattern. PrintServiceImpl: two new cases in the buildModel switch, putLetterOfAcceptanceModel, putEnhancementRequestModel, putInsurancePatientBlock, buildEnhancementBreakdownHtml (groups active charge lines net of discount; excludes CANCELLED lines). InsuranceJpaRepository injected. NOT RENDERED - needs a running app to confirm placeholder substitution.
- [x] ID-006 Frontend insuranceDesk.ts pure logic (stage unlocking, card expiry, validation, totals) + tests + API client
- [x] ID-007 Frontend UI: stage timeline, stage forms, bill-link modal, checklist/dispatch/cheque/disallowance grids

### WO-021 — Phase 10 — Insurance MIS reports (10 reports)  (IN_PROGRESS, 1/2 tasks)
- [>] IR-001 InsuranceReportDataService (10 scoped queries) + InsuranceReportService + controller + scoping tests
      last note: Cross-tenant isolation test WRITTEN (WO-021 AC-3): InsuranceReportTenantIsolationTest, Testcontainers Postgres 16 + full Flyway, @DisabledIf(dockerNotAvailable) matching AgentGatewayIsolationIntegrationTest. Covers: (a) all 10 reports run as tenant B assert tenant A markers absent, and symmetrically, collected into ONE assertion listing every offender rather than failing on the first; (b) each tenant still sees its own rows, so a query returning nothing cannot pass isolation while being useless; (c) branch-pinned context excludes a sibling branch in the SAME tenant - the case where a tenant-only predicate looks right and is still wrong; (d) unscoped SUPERADMIN context still sees both tenants; (e) a CoverageIsExhaustive test comparing the test's report table against InsuranceReportService.getAvailableReports(), so an 11th report cannot silently escape the suite; (f) every report returns empty rather than throwing for a tenant with no claims. Fixture seeds a claim carried through ALL SEVEN stages plus a partial cheque and a second mid-flow claim, because a claim seeded only to stage 1 leaves the dispatch/settlement/pending reports empty and an empty report cannot leak. Caught while writing: charge_line_items.service_catalog_item_id has an FK to service_catalog_items which requires a service_categories row - a random UUID would have failed at insert. STILL UNVERIFIED: cannot run (gradle cannot bootstrap; also needs Docker). verify_java_static passes, check_conventions clean.
- [x] IR-002 Frontend: 10 insurance report cards, filter dialogs, report routing

### WO-022 — DPDP remediation — consent gate integrity: remove self-granting, capture consent for real  (IN_PROGRESS, 0/6 tasks)
- [>] C-001 V205: provenance column, SYSTEM_INFERRED backfill, consent_notices registry + seed
      last note: MIGRATION DDL VERIFIED (schema only). Postgres 16.15 installed in-sandbox; all 202 migrations V001-V214 replayed into a fresh database with psql -v ON_ERROR_STOP=1. Zero failures, 151 tables. NOT a restored production copy: this proves the DDL is valid and correctly ordered, and proves NOTHING about the backfill DML, which ran against empty tables. Java still uncompiled (no javac, Maven Central blocked).
- [>] C-002 ConsentService: provenance-aware hasConsent, notice resolution, capturedBy mandatory, warn/enforce mode
      last note: IMPLEMENTED, NOT COMPILED. hasConsent now requires live AND reliable provenance; null/unparseable provenance fails CLOSED to SYSTEM_INFERRED. grant() refuses SYSTEM_INFERRED outright and refuses STAFF_ATTESTED without capturedBy - that single check is what makes the old self-grant pattern impossible to rebuild. Added captureFromAttestation which hashes the REGISTRY text not client-supplied text. hms.consent.enforcement property defaults to enforce; warn mode meters without blocking. NEEDS ./gradlew test.
- [>] C-003 ConsentGate + ConsentAttestation + 409 CONSENT_REQUIRED handler
      last note: IMPLEMENTED, NOT COMPILED. ConsentGate centralises the decision; capturedBy read from AuditorAware not the request body. 409 CONSENT_REQUIRED handler added to GlobalExceptionHandler carrying purpose + notice text (hospital copy, no patient data; patient id deliberately not echoed). 409 not 403 because the caller IS authorised. NEEDS ./gradlew test.
- [>] C-004 Cut over AbhaService, PolicyDiscoveryService, PreAuthService + controllers and DTOs
      last note: IMPLEMENTED, NOT COMPILED. All 3 self-grant blocks removed (AbhaService, PolicyDiscoveryService.requestDiscoveryOtp, PreAuthService.submitPreAuth). CORRECTION TO WO-022: the work order claimed 4 sites; PolicyDiscoveryService:186 (checkCoverage) only ever called requireConsent with no self-grant, so it was 3 not 4. checkCoverage was still routed through ConsentGate for consistency. Signatures gained a ConsentAttestation param; 3 DTOs and 3 controllers updated. Static verify: 909 files parse, all com.hms imports resolve. NEEDS ./gradlew test.
- [>] C-005 Metrics, inferred-remaining gauge, alerts, dashboard row
      last note: IMPLEMENTED. alerts-dpdp.yml with 10 rules across consent, rights and Rule 6 safeguards; wired into prometheus.yml rule_files and mounted in docker-compose.logging.yml. Added hms_consent_enforcement_warn_mode gauge via @PostConstruct so an alert can catch a gate left switched off. ALL THREE YAML FILES PARSE - this is the only part of the DPDP work that is actually verified, since YAML needs no toolchain. NOT DONE: Grafana dashboard JSON. Alert rules matter more than a panel; a panel needs someone looking at it.
- [>] C-006 Frontend ConsentGateModal wired into the three forms
      last note: FRONTEND VERIFIED, BACKEND NOT. npm install + npx tsc --noEmit: this card's files are type-clean. npx vitest run: 258 passed / 2 failed / 13 files; both failures are LoginPage.test.tsx, which last changed 2026-07-15 ('login restriction') against a campaign dated 2026-08-31, so they pre-date this work by six weeks and are not a regression - the component now sends forceLogout:false and the assertion was never updated. NOT marked DONE: this card is a vertical slice whose backend half has never compiled, so a green frontend proves half of it.

### WO-024 — DPDP remediation — data principal rights: erasure and correction end to end  (IN_PROGRESS, 0/6 tasks)
- [>] D-001 V206: patient_id on hitl_escalations + agent_idempotency_keys, request lifecycle columns, ERASURE_REQUEST feature key
      last note: MIGRATION DDL VERIFIED (schema only). Postgres 16.15 installed in-sandbox; all 202 migrations V001-V214 replayed into a fresh database with psql -v ON_ERROR_STOP=1. Zero failures, 151 tables. NOT a restored production copy: this proves the DDL is valid and correctly ordered, and proves NOTHING about the backfill DML, which ran against empty tables. Java still uncompiled (no javac, Maven Central blocked).
- [>] D-002 ErasureService rewrite: 24-store registry, corrected SQL, legacy unattributable sweep
      last note: BLIND SPOT IN THE COMPLETENESS CHECK. build_inventory.py flags a store as missing an erasure strategy only when it carries a patient_id column. Two tables identify the data principal by another name: agent_tool_invocations.target_entity_id (registered, and allowlisted in the script's other_key set) and pii_disclosure_audit.subject_id (NOT registered and NOT allowlisted, so the check can never flag it). pii_disclosure_audit holds a per-subject record of who accessed whose PHI; it has a 400-day retention policy but no erasure strategy, so an erasure request leaves those rows behind. That may well be the right answer - it is an audit trail, same argument as consent_records - but it is currently an accident of column naming rather than a recorded decision. Either register it as RETAIN with a stated reason, or add subject_id to the script's key detection. Do not just add it to other_key: that hides the question instead of answering it.
- [>] D-003 DataPrincipalRightsService: intake, verification gate, execution, refusal, overdue clock
      last note: IMPLEMENTED, NOT COMPILED. DataPrincipalRightsService is new. sweep() now refuses to run when requester_verified_at is null, enforced in BOTH the service and a DB CHECK constraint. 90-day statutory clock with a daily overdue job and gauge. Refusal requires a reason (truncated to the 500-char column, not dropped). Idempotent intake and execution.
- [>] D-004 DataPrincipalRightsController + DTOs (first endpoints in api/compliance)
      last note: IMPLEMENTED, NOT COMPILED. First endpoints ever in api/compliance - the package was empty while V179 seeded ERASURE_MANAGE and TenantService provisioned it, i.e. a permission guarding nothing. 7 endpoints. Split ERASURE_REQUEST (reception, intake+read) from ERASURE_MANAGE (verify, execute, reject) because taking a request and running an irreversible sweep are different risks. Convention scanner caught that ERASURE_REQUEST was seeded in V206 but NOT wired into TenantService for NEW tenants - fixed. No bulk erasure endpoint, deliberately. Response DTOs carry no decrypted patient data.
- [>] D-005 Populate patient_id at HITL and idempotency write sites
      last note: NOW COMPLETE. Only one idempotency call site existed (AgentToolController book_slot) and BookSlotToolRequest already carried patientId, so it was a one-line wire-up. Verified by grep: 1 caller, now using the 6-arg overload.
- [>] D-006 Frontend rights queue + receipt view
      last note: FRONTEND VERIFIED, BACKEND NOT. npm install + npx tsc --noEmit: this card's files are type-clean. npx vitest run: 258 passed / 2 failed / 13 files; both failures are LoginPage.test.tsx, which last changed 2026-07-15 ('login restriction') against a campaign dated 2026-08-31, so they pre-date this work by six weeks and are not a regression - the component now sends forceLogout:false and the assertion was never updated. NOT marked DONE: this card is a vertical slice whose backend half has never compiled, so a green frontend proves half of it.

### WO-023 — DPDP remediation — consent management surface and portal consent capture  (IN_PROGRESS, 0/5 tasks)
- [>] E-001 V207 + PORTAL_SELF_ACCESS purpose + CONSENT_VIEW feature key + TenantService wiring
      last note: MIGRATION DDL VERIFIED (schema only). Postgres 16.15 installed in-sandbox; all 202 migrations V001-V214 replayed into a fresh database with psql -v ON_ERROR_STOP=1. Zero failures, 151 tables. NOT a restored production copy: this proves the DDL is valid and correctly ordered, and proves NOTHING about the backfill DML, which ran against empty tables. Java still uncompiled (no javac, Maven Central blocked).
- [>] E-002 ConsentController: history, status, grant, withdraw, notice + DTOs
- [>] E-003 Portal self-registration records PATIENT_DIGITAL consent (closes S2-03)
      last note: IMPLEMENTED, NOT COMPILED. Closes S2-03. Portal self-registration now writes a real consent record with provenance PATIENT_DIGITAL and capturedBy null - correct, because the patient ticked the box themselves and no staff member attested. Wrapped in try/catch that logs ERROR and meters rather than rolling back a successful registration; an unrecorded consent is a gap but destroying a completed registration over it is worse.
- [>] E-004 Frontend PatientConsentPanel + consentApi + types
      last note: FRONTEND VERIFIED, BACKEND NOT. npm install + npx tsc --noEmit: this card's files are type-clean. npx vitest run: 258 passed / 2 failed / 13 files; both failures are LoginPage.test.tsx, which last changed 2026-07-15 ('login restriction') against a campaign dated 2026-08-31, so they pre-date this work by six weeks and are not a regression - the component now sends forceLogout:false and the assertion was never updated. NOT marked DONE: this card is a vertical slice whose backend half has never compiled, so a green frontend proves half of it.
- [ ] E-005 Notice languages: Tamil and Hindi seeds

### WO-028 — DPDP remediation — scanner backlog adjudication (71 HIGH)  (IN_PROGRESS, 2/9 tasks)
- [x] Q-001 Adjudicate all 71 HIGH findings; produce WO-028-ADJUDICATION.md
- [>] Q-002 CRITICAL: ABDM callback signature verification + tenant scoping
      last note: LEDGER REPAIR: card id changed from H-002 to Q-002. The old id collided campaign-wide and ledger.py resolved it to an older card in another work order, so this card was unreachable and any command aimed at it hit the wrong one. Documents updated in the same change.
- [>] Q-003 Encrypt visits.diagnosis, nhcx diagnosis_code/text, pharmacy customer_phone (V208)
      last note: MIGRATION DDL VERIFIED (schema only). Postgres 16.15 installed in-sandbox; all 202 migrations V001-V214 replayed into a fresh database with psql -v ON_ERROR_STOP=1. Zero failures, 151 tables. NOT a restored production copy: this proves the DDL is valid and correctly ordered, and proves NOTHING about the backfill DML, which ran against empty tables. Java still uncompiled (no javac, Maven Central blocked).
- [>] Q-004 Fix 3 real PII-in-exception-message leaks in PiiMigrationRunner
      last note: LEDGER REPAIR: card id changed from H-004 to Q-004. The old id collided campaign-wide and ledger.py resolved it to an older card in another work order, so this card was unreachable and any command aimed at it hit the wrong one. Documents updated in the same change.
- [>] Q-005 V208 forward backfill of features across all tenants
      last note: MIGRATION DDL VERIFIED (schema only). Postgres 16.15 installed in-sandbox; all 202 migrations V001-V214 replayed into a fresh database with psql -v ON_ERROR_STOP=1. Zero failures, 151 tables. NOT a restored production copy: this proves the DDL is valid and correctly ordered, and proves NOTHING about the backfill DML, which ran against empty tables. Java still uncompiled (no javac, Maven Central blocked).
- [>] Q-006 Deterministic search token for PasswordResetOtpEntity.email + tenant-scoping decision
      last note: MIGRATION DDL VERIFIED (schema only). Postgres 16.15 installed in-sandbox; all 202 migrations V001-V214 replayed into a fresh database with psql -v ON_ERROR_STOP=1. Zero failures, 151 tables. NOT a restored production copy: this proves the DDL is valid and correctly ordered, and proves NOTHING about the backfill DML, which ran against empty tables. Java still uncompiled (no javac, Maven Central blocked).
- [>] Q-007 Register 4 newly-encrypted columns with PiiMigrationRunner to encrypt existing plaintext
      last note: LEDGER REPAIR: card id changed from F-002 to Q-007. The old id collided campaign-wide and ledger.py resolved it to an older card in another work order, so this card was unreachable and any command aimed at it hit the wrong one. Documents updated in the same change.
- [ ] F-003 Confirm ABDM callback signature scheme against the gateway contract
- [x] F-004 Per-file exclusion comments for 16 child-table entities

### WO-026 — DPDP remediation — breach detection and notification (s. 8(6), Rule 7)  (IN_PROGRESS, 0/6 tasks)
- [>] I-001 V209 security_incidents + incident_affected_principals + 3 feature keys
      last note: MIGRATION DDL VERIFIED (schema only). Postgres 16.15 installed in-sandbox; all 202 migrations V001-V214 replayed into a fresh database with psql -v ON_ERROR_STOP=1. Zero failures, 151 tables. NOT a restored production copy: this proves the DDL is valid and correctly ordered, and proves NOTHING about the backfill DML, which ran against empty tables. Java still uncompiled (no javac, Maven Central blocked).
- [>] I-002 SecurityIncidentService: raise, scope, contain, notify, close, overdue clocks
      last note: IMPLEMENTED, NOT COMPILED. Deliberately does NOT auto-notify the Board - filing is an irreversible external act with legal weight and an automated false positive would be its own incident. detectedAt is never silently reset to now(): an incident discovered Monday and filed Tuesday is a Monday incident, and resetting hands back hours against a 72h obligation. draftPrincipalNotice generates all four Rule 7 elements; consequences is the one dropped under pressure and is the part that lets someone decide whether to act. Overdue check runs HOURLY not daily - a 72h obligation checked daily can be a third breached before anyone is told.
- [>] I-003 CrossTenantAccessDetector + GlobalExceptionHandler printStackTrace fix
      last note: IMPLEMENTED, NOT COMPILED. FOUND WHILE BUILDING: the cross-tenant handler called ex.printStackTrace() - stderr, outside the structured pipeline, so the one signal worth having never reached Loki and nothing counted it. The isolation guard worked and nobody would have known it fired. Also logged ex.getMessage(), which can quote identifiers from two tenants in one line. Same bug in the AccessDenied handler; both fixed. Detector escalates at 3 attempts in 15 min - fires exactly ON the threshold so a sustained attack raises one incident not one per request. Never throws: a detector that breaks the request it observes turns a handled security event into a 500.
- [>] I-004 SecurityIncidentController + DTOs
      last note: IMPLEMENTED, NOT COMPILED. INCIDENT_RAISE deliberately wide (clinical + reception): a near-miss nobody can file is one nobody learns from. INCIDENT_NOTIFY narrowest. No endpoint files with the Board automatically - it records that a human did.
- [>] I-005 7 breach and security alert rules in alerts-dpdp.yml
      last note: IMPLEMENTED AND YAML-VERIFIED. 17 rules across 4 groups now. Covers both Board clocks, cross-tenant bursts and single attempts, and all three ABDM verification outcomes including secret-missing (fails closed = callbacks silently dropped) and bypass-enabled.
- [>] I-006 Frontend incident register UI
      last note: FRONTEND VERIFIED, BACKEND NOT. npm install + npx tsc --noEmit: this card's files are type-clean. npx vitest run: 258 passed / 2 failed / 13 files; both failures are LoginPage.test.tsx, which last changed 2026-07-15 ('login restriction') against a campaign dated 2026-08-31, so they pre-date this work by six weeks and are not a regression - the component now sends forceLogout:false and the assertion was never updated. NOT marked DONE: this card is a vertical slice whose backend half has never compiled, so a green frontend proves half of it.

### WO-027 — DPDP remediation — grievance redressal and published contact point (s. 8(9), Rule 13)  (IN_PROGRESS, 0/6 tasks)
- [>] J-001 V210 grievances + grievance_events + compliance_contacts + 3 feature keys
      last note: MIGRATION DDL VERIFIED (schema only). Postgres 16.15 installed in-sandbox; all 202 migrations V001-V214 replayed into a fresh database with psql -v ON_ERROR_STOP=1. Zero failures, 151 tables. NOT a restored production copy: this proves the DDL is valid and correctly ordered, and proves NOTHING about the backfill DML, which ran against empty tables. Java still uncompiled (no javac, Maven Central blocked).
- [>] J-002 GrievanceService: intake, acknowledge, assign, resolve, escalate, SLA clocks
      last note: IMPLEMENTED, NOT COMPILED. patientId is NULLABLE on intake on purpose - requiring a match first would be a tidy way of never recording the inconvenient complaints, and someone complaining you hold their data wrongly may not appear the way you expect. Resolution text is mandatory: a status change dressed up as an answer is indistinguishable from being ignored. Escalation to the Board is RECORDED not treated as failure - they are entitled to at any point - but the rate is the truest measure of whether the mechanism is effective. Body/resolution/notes/complainant_contact all encrypted; a complaint is a person's account of their own care.
- [>] J-003 GrievanceController + public unauthenticated contact endpoint
      last note: IMPLEMENTED, NOT COMPILED. GET /compliance/grievances/contact/public is permitAll AND added to SecurityConfig - the annotation alone would not have worked. Unauthenticated by necessity: a contact point nobody can read without an account has not been published, and the people most likely to need it are those who cannot or will not log in. Serves organisational contact info only, read-only, tenant taken as a parameter because there is no session to infer it from. GRIEVANCE_RAISE granted to clinical+reception: a complaint only an admin can log is one that gets talked out of existence at the desk.
- [>] J-004 4 grievance SLA alert rules
      last note: IMPLEMENTED AND YAML-VERIFIED. 21 rules across 5 groups now. Includes GrievanceUnacknowledged (a complaint nobody replied to is the worst kind and the most likely to reach the Board) and GrievanceEscalationRateRising.
- [>] J-005 Frontend grievance queue + contact management UI
      last note: FRONTEND VERIFIED, BACKEND NOT. npm install + npx tsc --noEmit: this card's files are type-clean. npx vitest run: 258 passed / 2 failed / 13 files; both failures are LoginPage.test.tsx, which last changed 2026-07-15 ('login restriction') against a campaign dated 2026-08-31, so they pre-date this work by six weeks and are not a regression - the component now sends forceLogout:false and the assertion was never updated. NOT marked DONE: this card is a vertical slice whose backend half has never compiled, so a green frontend proves half of it.
- [>] J-006 Publish a contact for every existing tenant
      last note: PARTIALLY ADDRESSED. ComplianceContactCoverageCheck reports tenants with no published contact at startup and daily, logs at ERROR per tenant, publishes hms_tenants_without_contact, and alerts after 1h. It does NOT fix the gap - nobody but each hospital knows its own contact details. What it does is stop the gap being INVISIBLE, which is the property that lets an unmet obligation survive a year of deployments. Daily as well as at startup because tenants are onboarded between restarts and those are the ones most likely to lack a contact.

### WO-030 — DPDP remediation — data inventory, DPIA and compliance documentation  (IN_PROGRESS, 4/5 tasks)
- [x] K-001 Data inventory / RoPA from source enumeration
- [x] K-002 build_inventory.py regeneration script
- [x] K-003 DPIA draft for legal review
- [>] K-004 Draft notice text for the 7 consent purposes
      last note: IMPLEMENTED (migration untested), DOCUMENT DONE. .hms-agent/DRAFT-CONSENT-NOTICES.md plus V211 seeding all 7 as version v2.0-draft. Deliberately notice_state=DRAFT not ACTIVE: marking them ACTIVE would silence hms_consent_notice_draft_served_total, the one metric showing how many patients consent against unapproved text, and losing that signal by improving the text would be the wrong trade. ConsentService.activeNotice orders DRAFT by effective_from DESC so v2.0-draft is served ahead of v1.0 immediately - better text reaches patients now, metric keeps counting until a human approves. [RETENTION] and [CONTACT] left as visible placeholders on purpose: a plausible guess gets skimmed past, a bracket does not. TREATMENT notice is deliberately blunt that records cannot be fully deleted on request, because the erasure receipt tells the patient exactly that and notice must match behaviour.
- [x] K-005 Deployment runbook for V205-V210

### WO-025 — DPDP remediation — retention engine and storage limitation (s. 8(7))  (IN_PROGRESS, 0/6 tasks)
- [>] L-001 V213 retention_policies + runs + run_items + RETENTION_MANAGE
      last note: MIGRATION DDL VERIFIED (schema only). Postgres 16.15 installed in-sandbox; all 202 migrations V001-V214 replayed into a fresh database with psql -v ON_ERROR_STOP=1. Zero failures, 151 tables. NOT a restored production copy: this proves the DDL is valid and correctly ordered, and proves NOTHING about the backfill DML, which ran against empty tables. Java still uncompiled (no javac, Maven Central blocked).
- [>] L-002 RetentionService with four independent safety brakes
      last note: RETENTION SQL VERIFIED AGAINST THE REAL SCHEMA (schema only, still uncompiled). New script .hms-agent/scripts/check_retention_sql.py parses the six seeded policies straight out of V213's VALUES block (rather than retyping them, so it stays honest if the seed changes) and executes both statements RetentionService would build for each - countMatching and apply - against the replayed schema inside rolled-back transactions. 12/12 valid, every one tenant-scoped, no policy targets a NEVER_SWEEP store, all identifiers pass SAFE_IDENTIFIER. This is the @PostConstruct validation path that had never executed. Unlike ErasureService it came back clean. NOTE on hitl_escalations: its date column resolved_at is NULLABLE, so unresolved escalations are never swept by design - correct, but it means an escalation that is never resolved retains its transcript indefinitely and no policy will reach it. Worth a bounded fallback when counsel sets the periods (L-005).
- [>] L-003 RetentionController - no live manual trigger
      last note: IMPLEMENTED, NOT COMPILED. Deliberately NO endpoint that runs policies live on demand. The only manual trigger is /preview which forces dry-run regardless of policy config, so it is safe to call even against armed policies. Destruction happens on a schedule after someone read a preview and armed the policy - not because a button was available on a bad afternoon. A test asserts the controller never calls execute(null) or execute(FALSE).
- [>] L-004 4 retention alert rules
      last note: IMPLEMENTED AND YAML-VERIFIED. 25 rules across 6 groups now. RetentionNeverArmed fires after 90 days of everything still in dry-run - not an error, but a deployment that stays there forever is one where s.8(7) is still unmet, and that would otherwise be invisible.
- [ ] L-005 Legal review of retention periods
- [>] L-006 Frontend retention policy admin UI
      last note: FRONTEND VERIFIED, BACKEND NOT. npm install + npx tsc --noEmit: this card's files are type-clean. npx vitest run: 258 passed / 2 failed / 13 files; both failures are LoginPage.test.tsx, which last changed 2026-07-15 ('login restriction') against a campaign dated 2026-08-31, so they pre-date this work by six weeks and are not a regression - the component now sends forceLogout:false and the assertion was never updated. NOT marked DONE: this card is a vertical slice whose backend half has never compiled, so a green frontend proves half of it.

### WO-029 — DPDP remediation — Rule 6 hardening (JSONB encryption; MFA and key rotation deferred)  (IN_PROGRESS, 0/3 tasks)
- [>] U-001 EncryptedJsonMapConverter + V214 + Patient JSONB encryption
      last note: GAP FOUND, NOT FIXED (needs Gate 1). V214's own rationale says patients.pediatric_data 'was the only place in the patient record still storing [paediatric data] in the clear'. That is not true. A separate table patient_pediatric (V010) holds its own pediatric_data JSONB column, still plaintext, mapped by no JPA entity - it appears only in ErasureService's registry and RetentionService's NEVER_SWEEP list. Same Rule 12 category of data, same argument for encrypting it, missed because the work was scoped by entity and this table has no entity. Fixing it needs a new migration plus a converter and is out of scope for this card; raising it rather than silently widening WO-029.
- [ ] U-002 MFA for privileged users (Rule 6)
- [ ] U-003 Exercise key rotation

### WO-031 — Phase 6 — DPDP remediation — purpose limitation: remove the ungated marketing patient export  (IN_PROGRESS, 0/3 tasks)
- [>] B-001 Remove getPatientsForMaketing and getPatientBySearch from PatientController
      last note: IMPLEMENTED, NOT COMPILED. Both endpoints removed from PatientController and replaced with a comment block stating what they did, why they went, and the conditions for rebuilding - so a future reader does not restore them by reverting. DELETED RATHER THAN GATED, deliberately: gating adds a control around a bulk decryption path, deletion removes the path. Grounds: no caller anywhere in the repository (backend, frontend, mobile, agent-service, docs, tests - only the declarations themselves), and both advertised five filters they ignored, so nothing depending on the documented behaviour can have been working. RESIDUAL RISK, stated plainly: this does not prove no EXTERNAL caller exists. An unknown integration would now get 404. That is visible and revertible; an ungated bulk PII export is neither. VERIFIED OFFLINE: verify_java_static 955 files, 0 syntax errors, all com.hms imports resolve; imports List/PatientResponse/UUID all still used elsewhere in the file so none are orphaned; convention scan 604 vs baseline 605, HIGH unchanged at 60, diff shows only the expected INFO findings removed and the surviving endpoints re-reported at shifted line numbers. NOT MARKED DONE: no compiler.
- [ ] B-002 Regression test: no MARKETING-permissioned endpoint returns patient PII
- [ ] B-003 Decide the fate of the MARKETING ConsentPurpose, notice and feature key

### WO-032 — Independent review remediation — F1 branch_id schema defect, F2 s.8(9) contact, F3 minority from DOB, F4 logging  (IN_PROGRESS, 0/6 tasks)
- [>] X-001 V215: branch_id on the six compliance tables + tenant-wide filters on their entities
      last note: IMPLEMENTED, NOT COMPILED. No javac in this environment and Maven Central is outside the sandbox allowlist. Verified offline: verify_java_static 960 files, 0 syntax errors, all com.hms imports resolve; all 203 migrations (V001-V215) replayed into a fresh Postgres 16.15 with ON_ERROR_STOP=1, zero failures, 151 tables; check_entity_schema against that replayed schema shows all six F1 tables now PASS; frontend npx tsc --noEmit clean for every file touched (the 7 remaining errors are all in DoctorCalendarPage.tsx, untouched); convention scan 606 vs baseline 604, both new findings adjudicated false positive.
- [>] X-002 Entity/schema check: AuditableEntitySchemaTest + check_entity_schema.py
      last note: IMPLEMENTED, NOT COMPILED. No javac in this environment and Maven Central is outside the sandbox allowlist. Verified offline: verify_java_static 960 files, 0 syntax errors, all com.hms imports resolve; all 203 migrations (V001-V215) replayed into a fresh Postgres 16.15 with ON_ERROR_STOP=1, zero failures, 151 tables; check_entity_schema against that replayed schema shows all six F1 tables now PASS; frontend npx tsc --noEmit clean for every file touched (the 7 remaining errors are all in DoctorCalendarPage.tsx, untouched); convention scan 606 vs baseline 604, both new findings adjudicated false positive.
- [>] X-003 Grievance contact mandatory at tenant onboarding
      last note: IMPLEMENTED, NOT COMPILED. No javac in this environment and Maven Central is outside the sandbox allowlist. Verified offline: verify_java_static 960 files, 0 syntax errors, all com.hms imports resolve; all 203 migrations (V001-V215) replayed into a fresh Postgres 16.15 with ON_ERROR_STOP=1, zero failures, 151 tables; check_entity_schema against that replayed schema shows all six F1 tables now PASS; frontend npx tsc --noEmit clean for every file touched (the 7 remaining errors are all in DoctorCalendarPage.tsx, untouched); convention scan 606 vs baseline 604, both new findings adjudicated false positive.
- [>] X-004 Minority derived from date of birth, not declared
      last note: IMPLEMENTED, NOT COMPILED. No javac in this environment and Maven Central is outside the sandbox allowlist. Verified offline: verify_java_static 960 files, 0 syntax errors, all com.hms imports resolve; all 203 migrations (V001-V215) replayed into a fresh Postgres 16.15 with ON_ERROR_STOP=1, zero failures, 151 tables; check_entity_schema against that replayed schema shows all six F1 tables now PASS; frontend npx tsc --noEmit clean for every file touched (the 7 remaining errors are all in DoctorCalendarPage.tsx, untouched); convention scan 606 vs baseline 604, both new findings adjudicated false positive.
- [>] X-005 Logging: safe profile-less defaults, DEBUG moved to dev, logs/ ignored
      last note: IMPLEMENTED, NOT COMPILED. No javac in this environment and Maven Central is outside the sandbox allowlist. Verified offline: verify_java_static 960 files, 0 syntax errors, all com.hms imports resolve; all 203 migrations (V001-V215) replayed into a fresh Postgres 16.15 with ON_ERROR_STOP=1, zero failures, 151 tables; check_entity_schema against that replayed schema shows all six F1 tables now PASS; frontend npx tsc --noEmit clean for every file touched (the 7 remaining errors are all in DoctorCalendarPage.tsx, untouched); convention scan 606 vs baseline 604, both new findings adjudicated false positive.
- [ ] X-006 F5: AreaEntity maps a dropped table; customers missing three mapped columns

## Decisions on record

- {'at': '2026-08-31T13:55:13+00:00', 'work_order': 'WO-032', 'decision': 'The six F1 tables are tenant-wide (branchFilter 1=1); branch_id exists for the mapping and stays NULL.', 'reasoning': "Branch-scoping an erasure request would let the statutory clock run against a record another branch's compliance officer cannot see. Branch-scoping the Rule 7 affected-principals list would mean under-notification. Retention policy is a hospital-level legal determination. grievance_events hangs off a branch-scoped parent that remains the access control, and a half-visible audit trail is worse than either extreme."}
- {'at': '2026-08-31T13:55:13+00:00', 'work_order': 'WO-032', 'decision': 'A minor/DOB contradiction is refused, not corrected.', 'reasoning': "Silent correction produces a consent record whose provenance is 'the server decided' while claiming a person attested to it. A refused capture is recoverable; a false attestation is the defect WO-022 already had to unwind."}
