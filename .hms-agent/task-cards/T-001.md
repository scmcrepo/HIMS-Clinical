# T-001 — Remove HmsPermissionEvaluator debug-file write

| | |
|---|---|
| **Work order** | WO-001 |
| **Status** | TODO |
| **Depends on** | — |
| **Parallelisable with** | T-002, T-003 |
| **Estimate** | S |

## Goal

Permission checks perform zero filesystem I/O. The hot authorization path stops writing unrotated, unmasked user and tenant data to a hardcoded developer path.

## Files touched

- `backend/src/main/java/com/hms/security/HmsPermissionEvaluator.java`
- `backend/src/test/java/com/hms/security/HmsPermissionEvaluatorTest.java`
- `.gitignore`

## Implementation notes

- Delete the Files.writeString block in hasPermission(). The existing log.warn on DENY already covers the diagnostic need; if more is wanted, use log.debug through the existing SLF4J logger so Promtail sees it and rotation applies.
- Remove the committed evaluator_debug.log (255 KB) and add *.log plus evaluator_debug.log to .gitignore. Do not go further into the committed-logs backlog — application.log, bootrun.log and friends are a separate cleanup.
- This is the only card that touches pre-existing code rather than adding new code, so keep the diff surgical and reviewable.

## Definition of done

Card-specific:
- [ ] A permission check creates no file. Test asserts the previously hardcoded path does not exist after invoking hasPermission().
- [ ] DENY still emits a WARN through SLF4J with username, tenant, roles, feature.
- [ ] Existing permission-evaluator tests still pass; no behaviour change to allow/deny outcomes.
- [ ] evaluator_debug.log removed from git; .gitignore prevents recurrence.

Standing gates (references/verification.md §1):
- [ ] Build and tests green (backend, frontend)
- [ ] Tenant isolation verified with a cross-tenant absence test
- [ ] RBAC: @PreAuthorize + feature seeded for existing tenants + wired into tenant provisioning + 403 test with a non-privileged tenant user
- [ ] PII encrypted, search token where needed, nothing personal in logs
- [ ] Audit record written for state changes
- [ ] Observability implemented and asserted in a test
- [ ] Migration idempotent, next free version, rollback documented

## Evidence

_Filled in at completion. What was run, what passed._

## Blockers

_Recorded via `ledger.py block`._
