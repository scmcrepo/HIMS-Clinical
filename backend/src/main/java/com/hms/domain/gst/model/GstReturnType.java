package com.hms.domain.gst.model;

/** The returns this module can build. */
public enum GstReturnType {
    /** Outward supplies, invoice-level. */
    GSTR1,
    /** Summary return — totals only. */
    GSTR3B
}
